export const packageName = "@jawstack/cli";

export function getHelpText(): string {
  return `jawstack

Usage:
  jawstack --help

Commands will be implemented in later MVP phases.`;
}

export function describePackage(): string {
  return `${packageName} package scaffold`;
}
