export const packageName = "create-jawstack";

export function getHelpText(): string {
  return `create-jawstack

Usage:
  create-jawstack <app-name>

Project generation will be implemented in a later MVP phase.`;
}

export function describePackage(): string {
  return `${packageName} package scaffold`;
}
