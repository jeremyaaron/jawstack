# create-jawstack

Project generator for JawStack applications.

```sh
npx create-jawstack my-app
```

The MVP generator copies the Angular resource workflow starter and includes the Work Request
example by default.
